ShowFilters1 = 
--Measure produces list of filters selected per column  plus line breaks, with selections higher than three being suppressed

var maxf = 3
return
---Staff Group
IF(
    ISFILTERED(DIM_Metric_Mapping[Staff Group]), 
    VAR var1 = VALUES(DIM_Metric_Mapping[Staff Group])
    VAR var2 = CONCATENATEX(var1, DIM_Metric_Mapping[Staff Group], ", ")
    VAR var3 = COUNTROWS(var1)
    return IF(var3 > maxf, "Multiple selections" & UNICHAR(10),var2 & UNICHAR(10))
)&
---Category (i.e. job role)
IF(
    ISFILTERED(DIM_Metric_Mapping[Category]), 
    VAR var1 = VALUES(DIM_Metric_Mapping[Category])
    VAR var2 = CONCATENATEX(var1, DIM_Metric_Mapping[Category], ", ")
    VAR var3 = COUNTROWS(var1)
    return IF(var3 > maxf, "Multiple selections" & UNICHAR(10),var2 & UNICHAR(10))
)&
---Subcategory (i.e. job role)
IF(
    ISFILTERED(DIM_Metric_Mapping[Subcategory]), 
    VAR var1 = VALUES(DIM_Metric_Mapping[Subcategory])
    VAR var2 = CONCATENATEX(var1, DIM_Metric_Mapping[Subcategory], ", ")
    VAR var3 = COUNTROWS(var1)
    return IF(var3 > maxf, "Multiple selections" & UNICHAR(10),var2 & UNICHAR(10))
)&
---Place
IF(
    ISFILTERED(DIM_PracticeMapping[PlaceName]), 
    VAR var1 = VALUES(DIM_PracticeMapping[PlaceName])
    VAR var2 = CONCATENATEX(var1, DIM_PracticeMapping[PlaceName], ", ")
    VAR var3 = COUNTROWS(var1)
    return IF(var3 > maxf, "Multiple selections" & UNICHAR(10),var2 & UNICHAR(10))
)&
---PCN
IF(
    ISFILTERED(DIM_PracticeMapping[PCNName]), 
    VAR var1 = VALUES(DIM_PracticeMapping[PCNName])
    VAR var2 = CONCATENATEX(var1, DIM_PracticeMapping[PCNName], ", ")
    VAR var3 = COUNTROWS(var1)
    return IF(var3 > maxf, "Multiple selections" & UNICHAR(10),var2 & UNICHAR(10))
)&
---Practice
IF(
    ISFILTERED(DIM_PracticeMapping[GeneralPracticeName]), 
    VAR var1 = VALUES(DIM_PracticeMapping[GeneralPracticeName])
    VAR var2 = CONCATENATEX(var1, DIM_PracticeMapping[GeneralPracticeName], ", ")
    VAR var3 = COUNTROWS(var1)
    return IF(var3 > maxf, "Multiple selections" & UNICHAR(10),var2 & UNICHAR(10))
)&
---Gender
IF(
    ISFILTERED(DIM_Metric_Mapping[Gender]), 
    VAR var1 = VALUES(DIM_Metric_Mapping[Gender])
    VAR var2 = CONCATENATEX(var1, DIM_Metric_Mapping[Gender], ", ")
    VAR var3 = COUNTROWS(var1)
    return IF(var3 > maxf, "Multiple selections" & UNICHAR(10),var2 & UNICHAR(10))
)&

---Patient Age
IF(
    ISFILTERED(DIM_Metric_Mapping_Patients[Age Range]), 
    VAR var1 = VALUES(DIM_Metric_Mapping_Patients[Age Range])
    VAR var2 = CONCATENATEX(var1, DIM_Metric_Mapping_Patients[Age Range], ", ")
    VAR var3 = COUNTROWS(var1)
    return IF(var3 > maxf, "Multiple selections" & UNICHAR(10),var2 & UNICHAR(10))
)&

---Patient Gender
IF(
    ISFILTERED(DIM_Metric_Mapping_Patients[Gender]), 
    VAR var1 = VALUES(DIM_Metric_Mapping_Patients[Gender])
    VAR var2 = CONCATENATEX(var1, DIM_Metric_Mapping_Patients[Gender], ", ")
    VAR var3 = COUNTROWS(var1)
    return IF(var3 > maxf, "Multiple selections" & UNICHAR(10),var2 & UNICHAR(10))
)


ShowFilters1_Headers = 
--Measure produces list of headers and line breaks for table to show filters selected

 VAR Maxf = 3
 Return
---Staff Group
IF(
    ISFILTERED(DIM_Metric_Mapping[Staff Group]),
    VAR var1 = COUNTROWS(VALUES(DIM_Metric_Mapping[Staff Group]))
    return
"Staff Group: " & UNICHAR(10)
)&

    ---Category (i.e. job role)
IF(
    ISFILTERED(DIM_Metric_Mapping[Category]), 
    VAR var1 = COUNTROWS(VALUES(DIM_Metric_Mapping[Category]))
    return
 "Category: " & UNICHAR(10)
)&

---Subcategory (i.e. job role)
IF(
    ISFILTERED(DIM_Metric_Mapping[Subcategory]),
    VAR var1 = COUNTROWS(VALUES(DIM_Metric_Mapping[Subcategory]))
    return
"Sub-Category: " & UNICHAR(10)
)&

---Place
IF(
    ISFILTERED(DIM_PracticeMapping[PlaceName]), 
    VAR var1 = COUNTROWS(VALUES(DIM_PracticeMapping[PlaceName]))
    return
"Place: " & UNICHAR(10)
)&

---PCN
IF(
    ISFILTERED(DIM_PracticeMapping[PCNName]),
    VAR var1 = COUNTROWS(VALUES(DIM_PracticeMapping[PCNName]))
    return
"PCN: " & UNICHAR(10)
)&

---Practice
IF(
    ISFILTERED(DIM_PracticeMapping[GeneralPracticeName]), 
    VAR var1 = COUNTROWS(VALUES(DIM_PracticeMapping[GeneralPracticeName]))
    return
"Practice: " & UNICHAR(10)
)&

---Gender
IF(
    ISFILTERED(DIM_Metric_Mapping[Gender]), 
    VAR var1 = COUNTROWS(VALUES(DIM_Metric_Mapping[Gender]))
    return
"Gender: " & UNICHAR(10)
)&

---Patient Age
IF(
    ISFILTERED(DIM_Metric_Mapping_Patients[Age Range]), 
    VAR var1 = COUNTROWS(VALUES(DIM_Metric_Mapping_Patients[Age Range]))
    return
"Patient Age Group: " & UNICHAR(10)
)&

---Patient Gender
IF(
    ISFILTERED(DIM_Metric_Mapping_Patients[Gender]), 
    VAR var1 = COUNTROWS(VALUES(DIM_Metric_Mapping_Patients[Gender]))
    return
"Patient Gender: " & UNICHAR(10)
)

ShowFilters1_Headers2 = IF(ISBLANK([ShowFilters1]), "None", [ShowFilters1_Headers])